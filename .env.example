# Optional. The app works without this, but answers are better with an LLM.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# MLflow
MLFLOW_TRACKING_URI=./mlruns
MLFLOW_EXPERIMENT_NAME=research-paper-ragops
